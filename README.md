# 🤖 AI Service Marketplace

Информативный маркетплейс AI-специалистов с интеграцией Supabase.

## 📋 Описание

Каталог AI-специалистов для заказа услуг по видео, дизайну, ботам, упаковке маркетплейсов и созданию сайтов. Проект включает:

- 🎯 5 категорий AI-услуг
- 👥 Каталог исполнителей с портфолио
- 📝 Единая форма заявки
- 🔐 Авторизация через email (magic link)
- 💾 База данных на Supabase
- 🎨 Современный дизайн в стиле neumorphism

## 🚀 Основные возможности

### Для пользователей:
- Просмотр каталога AI-специалистов
- Фильтрация по категориям
- Просмотр портфолио и кейсов
- Отправка заявок исполнителям

### Для исполнителей:
- Регистрация через email (passwordless)
- Создание профиля
- Загрузка портфолио и работ
- Получение заявок от клиентов

## 🛠 Технологии

- **Frontend:** HTML, CSS, JavaScript (Vanilla)
- **Backend:** Supabase (PostgreSQL)
- **Авторизация:** Supabase Auth (Magic Link)
- **Дизайн:** Neumorphism, адаптивная верстка
- **Локальный сервер:** Laravel Herd / Open Server

## 📁 Структура проекта

```
.
├── index.html           # Главная страница
├── auth.html           # Страница авторизации
├── add.html            # Личный кабинет (создание карточки)
├── apply.html          # Форма заявки
├── experts.html        # Каталог экспертов
├── css/
│   ├── custom.css      # Кастомные стили
│   └── tilda-*.css     # Стили Tilda
├── js/
│   └── *.js           # JavaScript файлы
└── images/            # Изображения
```

## ⚙️ Настройка

### 1. Клонирование репозитория

```bash
git clone https://github.com/swipeprod-arch/Ai-service.git
cd Ai-service
```

### 2. Настройка Supabase

В файлах `index.html` и `auth.html` найди секцию:

```javascript
window.AI_BIRZHA = {
  SUPABASE_URL: "ваш_url",
  SUPABASE_ANON_KEY: "ваш_ключ",
  
  // Измени эти URL на свои:
  LOCAL_URL: "http://localhost",
  PRODUCTION_URL: "https://yourdomain.com",
  
  ENVIRONMENT: "auto"  // auto, local, или production
};
```

### 3. Настройка базы данных

Таблицы в Supabase:
- `experts` - профили экспертов
- `expert_albums` - альбомы работ
- `expert_media` - медиафайлы

### 4. Локальный запуск

Используй любой веб-сервер:

**Laravel Herd (рекомендуется):**
```bash
herd link
herd open
```

**PHP встроенный сервер:**
```bash
php -S localhost:8000
```

**Open Server / XAMPP:**
Просто помести файлы в папку `htdocs`

## 🌐 Автоопределение окружения

Проект автоматически определяет, где он запущен:

- **Локально** (`.test`, `localhost`, `127.0.0.1`) → использует `LOCAL_URL`
- **На сервере** (любой другой домен) → использует `PRODUCTION_URL`

Можно установить вручную через `ENVIRONMENT`:
- `"auto"` - автоопределение (по умолчанию)
- `"local"` - всегда локальный URL
- `"production"` - всегда продакшн URL

## 🔐 Авторизация

Проект использует Supabase Auth с Magic Link:

1. Пользователь вводит email
2. Получает ссылку на почту
3. Переходит по ссылке → авторизован
4. Может создать профиль эксперта

## 📦 Деплой

### На Beget / обычный хостинг:

1. Загрузи все файлы через FTP
2. Измени `PRODUCTION_URL` на свой домен
3. Добавь свой домен в настройки Supabase (Authentication → URL Configuration)
4. Готово!

### Проверь:
- `.htaccess` должен быть переименован в `.htaccess` (с точкой в начале)
- Права на файлы: 644 для файлов, 755 для папок

## 🐛 Отладка

В консоли браузера будут логи:
```
🌍 Окружение: LOCAL
🔗 PUBLIC_ORIGIN: http://project13172919-.test
```

Это помогает понять, какой URL используется для редиректов.

## 📝 TODO

- [ ] Добавить поиск по экспертам
- [ ] Фильтрация по категориям
- [ ] Рейтинг и отзывы
- [ ] Чат с исполнителями
- [ ] Админ-панель

## 🤝 Контрибьюторы

- [@swipeprod-arch](https://github.com/swipeprod-arch) - основной разработчик

## 📄 Лицензия

MIT License

## 🔗 Ссылки

- [Supabase Documentation](https://supabase.com/docs)
- [Tilda Publishing](https://tilda.cc/)
- [Neumorphism Design](https://neumorphism.io/)

---

💡 **Совет:** Для быстрого старта измени только `PRODUCTION_URL` в конфиге и URL в Supabase!

